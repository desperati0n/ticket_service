注意事项
每次改动完成后，都必须创建一个对应的git commit，以便后续追踪和回滚
每次改动后，都必须编写或更新相关测试，并在交付给用户前，确保所有测试和验证全部通过

## 已拍板的项目技术与架构

### 业务场景

本项目是一个内部 IT 运维助手。员工通过自然语言提交资产故障或维修请求，例如：
“我的 Dell 显示器坏了，工号 10086，急用”。系统需要查员工信息、生成维修工单，并归档对话记录。

### 技术栈

- **FastAPI**：提供 HTTP API，目标接口为 `POST /ticket/stream`，通过 SSE 流式返回处理过程。
- **MySQL**：关系型业务数据存储，至少包含 `employees`、`assets`、`tickets` 三类表。
- **MongoDB**：存储半结构化的对话历史和工单处理日志，集合暂定为 `conversation_logs`。
- **LangChain**：封装工具与 Agent 路由；计划定义 `get_employee_by_id`、`create_ticket` 两个工具，并通过 `bind_tools` 绑定到 ChatModel。
- **ChatModel/LLM**：仅作为后续阶段的意图理解与工具路由组件；当前阶段不接入 AI，先完成确定性流程。
- **SSE**：将“请求接收 → 查询员工/资产 → 创建工单 → 写入对话日志 → 完成”过程逐步推送给客户端。

### 分阶段范围

当前阶段为**无 AI MVP**：保留未来 LangChain/ChatModel 的接口边界，但使用显式、可测试的规则或字段完成请求解析和流程编排，不调用外部模型。

后续阶段再接入自然语言意图匹配、工具调用和模型推理；接入后不得改变 MySQL/MongoDB 的核心数据契约和 SSE 事件契约。

### 数据职责

- MySQL 保存员工、资产、工单等需要事务和结构化查询的业务事实。
- MongoDB 保存原始对话、情绪/上下文等半结构化字段、工具调用中间步骤及工单处理日志。
- 工单初始状态统一为“待处理”（具体枚举值在建模问题确认后确定）。

### 设计约束

- 员工信息通过工号查询；员工不存在、资产不存在、重复提交等情况必须有明确错误事件和可测试行为。
- 创建工单与相关业务校验需要保证一致性；跨 MySQL 与 MongoDB 的写入采用可观测、可补偿的流程，不假设跨库事务。
- SSE 事件需要定义稳定的 `event`/`data` 格式、结束事件和错误事件。
- 所有新增或修改的代码必须配套测试，并在交付前通过全部测试。

### 环境变量与配置

- 项目配置统一通过根目录 `.env` 注入；数据库服务、连接参数、SSE 行为参数以及未来 ChatModel 的模型名、温度、最大输出等参数都必须提供对应环境变量。
- 不创建 `config.py`；配置读取可使用专门的 settings 模块，但不得把运行环境配置硬编码在业务代码中。
- `.env` 用于本地运行，禁止写入真实密钥；提交仓库时同时维护 `.env.example` 作为变量清单和默认示例。
